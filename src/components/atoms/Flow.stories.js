import React from "react"

import { Flow } from "./Flow"

export default {
  title: "MyPortFolioSite/Flow",
  component: Flow,
}

const Template = args => <Flow {...args} />

export const flow = Template.bind({})
