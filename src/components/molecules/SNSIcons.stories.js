import React from "react"

import { SNSIcons } from "./SNSIcons"

export default {
  title: "MyPortFolioSite/SNSIcons",
  component: SNSIcons,
}

const Template = args => <SNSIcons {...args} />

export const list = Template.bind({})
