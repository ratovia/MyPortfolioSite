import React from "react"

import { Menu } from "./Menu"

export default {
  title: "MyPortFolioSite/Menu",
  component: Menu,
}

const Template = args => <Menu {...args} />

export const menu = Template.bind({})
